model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  password  String
  role      String   @default("rt") // rt, bendahara, admin
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
